// Site-wide feature flags.
// Flip SHOW_WORKS back to true once case studies are ready — it will
// restore the "Selected Work" section on Home and the Work links in
// Header, Footer, and BottomNav. The /works and /work/:slug routes
// stay live either way, so nothing breaks if an old link is shared.
export const SHOW_WORKS = false;
